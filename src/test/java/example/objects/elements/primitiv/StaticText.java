package example.objects.elements.primitiv;

import example.base.BaseElement;
import org.openqa.selenium.WebElement;

public class StaticText extends BaseElement {
    public StaticText(WebElement element) {
        super(element);
    }
}
